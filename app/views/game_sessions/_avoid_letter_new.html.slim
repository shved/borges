= react_component('AvoidLetter',
  letter: @game_session.props[:letter],
  gameName: @game.name,
  gamePrompt: t('.game_prompt').sample,
  gameId: @game.id,
  authorSignedIn: current_author.present?)

